# Full-Text Search Project for Scientific Articles

## ⁧هدف پروژه⁩

⁧این پروژه برای ساخت یک سیستم جست‌وجوی تمام‌متن روی متادیتای مقالات علمی ⁦arXiv⁩ است. ورودی پروژه یک دیتاست ⁦JSONL⁩ شامل ⁦50⁩,⁦000⁩ مقاله است و هر مقاله اطلاعاتی مثل عنوان، چکیده، نویسندگان، دسته‌بندی علمی و تاریخ به‌روزرسانی دارد.⁩

⁧هدف نهایی فقط ذخیره‌کردن داده نیست. باید یک مسیر کامل داده تا ارزیابی ساخته شود:⁩

- ⁧داده خام بررسی و پاک‌سازی شود.⁩
- ⁧داده در یک موتور مناسب برای جست‌وجوی متنی ذخیره شود.⁩
- ⁧چند نوع جست‌وجو شامل ⁦keyword⁩، ⁦contain⁩ و ⁦fuzzy⁩ پیاده‌سازی شود.⁩
- ⁧معماری ترکیبی جست‌وجو ساخته شود.⁩
- ⁧کارایی سیستم با متریک‌های واقعی اندازه‌گیری شود.⁩
- ⁧تست‌های چندکلاینتی اجرا شود.⁩
- ⁧طراحی اولیه با طراحی بهینه‌شده مقایسه شود.⁩
- ⁧گزارش نهایی شامل نتایج، نمودارها، تحلیل و توجیه تصمیم‌ها آماده شود.⁩

⁧کاربرد عملی پروژه این است که کاربر بتواند بین هزاران مقاله علمی با عبارت‌های معمولی، عبارت‌های جزئی، عبارت‌های دارای غلط تایپی، فیلتر دسته‌بندی و تحلیل آماری جست‌وجو کند. بنابراین پروژه بیشتر شبیه یک موتور جست‌وجوی کوچک برای مقاله‌هاست تا یک ⁦CRUD⁩ ساده.⁩

## ⁧تقسیم‌بندی کل پروژه⁩

⁧پروژه را باید به ⁦10⁩ مرحله اصلی تقسیم کرد. ترتیب زیر منطقی است، چون هر مرحله خروجی مرحله قبل را مصرف می‌کند.⁩

⁧آخرین بازبینی وضعیت: ⁦2026-08-08⁩. تست‌ها با دستور ⁦`conda run --no-capture-output -n ai_agent python -m pytest -q`⁩ اجرا شدند: ⁦71 passed⁩ و ⁦2 subtests passed⁩. اعتبارسنجی دیتاست پاک‌شده هم ⁦passed⁩ است. ⁦Docker/Elasticsearch⁩ سالم و ⁦cluster health⁩ برابر ⁦green⁩ است. هر دو ⁦index⁩ زندهٔ ⁦`arxiv_papers_baseline`⁩ و ⁦`arxiv_papers_optimized`⁩ ساخته و با ⁦50,000⁩ سند بارگذاری شده‌اند. ⁦`scripts/smoke_test.py`⁩ زنده پاس شده، جست‌وجوی نمونه روی هر دو ⁦index⁩ پاس شده و ⁦`results/reproducibility_manifest.json`⁩ با وضعیت ⁦`passed`⁩ دوباره ساخته شده است. بنابراین کدها، گزارش‌ها، artifactهای ذخیره‌شده و محیط زندهٔ ⁦Elasticsearch⁩ برای ادامه یا تحویل آماده‌اند.⁩

⁧یادداشت ممیزی تحویل: گزارش‌های benchmark قدیمی نشان می‌دهند اجراهای ⁦baseline⁩ با ⁦Python 3.13.3⁩ و اجراهای ⁦optimized⁩ با ⁦Python 3.11.8⁩ ثبت شده‌اند. سایر مؤلفه‌های اصلی مقایسه، شامل ⁦Docker/Elasticsearch⁩، دیتاست، ⁦mapping⁩، ⁦seed⁩ و محدودیت منابع، در artifactها ثبت و قابل ردیابی‌اند. این موضوع در گزارش نهایی به‌عنوان محدودیت شفاف شده است؛ اگر مقایسهٔ زمانی کاملاً سخت‌گیرانه لازم باشد، باید همهٔ benchmarkهای دو نسخه با یک ⁦Python⁩ واحد دوباره اجرا شوند.⁩

⁧یادداشت ابزار اختیاری: یک رابط گرافیکی محلی برای دمو و جست‌وجوی دستی اضافه شده است: ⁦`scripts/search_ui.py`⁩. این UI الزام صورت پروژه نیست و در مقایسه‌ها یا متریک‌های رسمی استفاده نمی‌شود؛ فقط یک ابزار کمکی بدون وابستگی اضافه برای جست‌وجو روی ⁦`arxiv_papers_baseline`⁩ و ⁦`arxiv_papers_optimized`⁩ است.⁩

## ⁧مرحله ⁦1:⁩ پاک‌سازی و آماده‌سازی داده⁩

⁧وضعیت: انجام شده.⁩

⁧هدف این مرحله ساخت نسخه‌ای قابل اعتماد از دیتاست خام بود تا در مراحل بعدی بدون نگرانی از مشکل ⁦schema⁩، تاریخ، شناسه، متن خالی یا کاراکتر کنترلی استفاده شود.⁩

⁧کارهای انجام‌شده:⁩

- ⁧خواندن فایل ⁦JSONL⁩ به‌صورت خط‌به‌خط برای کنترل مصرف حافظه.⁩
- ⁧حذف احتمالی رکوردهای ⁦JSON⁩ نامعتبر یا غیرشیء.⁩
- ⁧حذف احتمالی رکوردهای بدون ⁦`paper_id`⁩، ⁦`title`⁩، ⁦`abstract`⁩ یا ⁦`update_date`⁩ معتبر.⁩
- ⁧پاک‌سازی متن‌ها با ⁦Unicode NFC.⁩⁩
- ⁧حذف نویسه‌های کنترلی و نامرئی.⁩
- ⁧یکسان‌سازی فاصله‌ها.⁩
- ⁧حفظ علائم علمی، ⁦URL⁩، ⁦LaTeX⁩ و ⁦punctuation.⁩⁩
- ⁧حفظ ⁦`authors`⁩ به‌صورت رشته.⁩
- ⁧تبدیل ⁦`categories`⁩ به آرایه‌ای بدون تکرار.⁩
- ⁧تضمین عضویت ⁦`primary_category`⁩ در ⁦`categories`⁩.⁩
- ⁧استخراج ⁦`year`⁩ از ⁦`update_date`⁩.⁩
- ⁧بازسازی ⁦`title_abstract`⁩ از ⁦`title + " " + abstract`⁩.⁩
- ⁧اعتبارسنجی مستقل خروجی.⁩

⁧خروجی این مرحله:⁩

- `dataset/arxiv_project_sample_50k_cleaned.jsonl`
- `dataset/DATASET.md`
- `scripts/clean_dataset.py`
- `scripts/validate_cleaned_dataset.py`
- `tests/test_clean_dataset.py`

⁧نکتهٔ قرارداد داده: نمونهٔ ⁦PDF⁩ صورت پروژه ممکن است فیلد ⁦`id`⁩ را نشان دهد، اما دیتاست واقعی این پروژه طبق ⁦`dataset/DATASET.md`⁩ از فیلد ⁦`paper_id`⁩ استفاده می‌کند. بنابراین در همهٔ مراحل ⁦indexing⁩، ⁦query⁩، حذف تکراری و گزارش‌دهی، شناسهٔ سند ⁦`paper_id`⁩ است.⁩

⁧معیار اتمام:⁩

- ⁧اعتبارسنج با وضعیت ⁦`passed`⁩ تمام شود.⁩
- ⁧تعداد رکوردهای خروجی مشخص و قابل دفاع باشد.⁩
- ⁧⁦schema⁩ خروجی ثابت باشد.⁩
- ⁧تمام رکوردها برای ورود به موتور جست‌وجو آماده باشند.⁩

## ⁧مرحله ⁦2:⁩ انتخاب تکنولوژی و طراحی محیط اجرا⁩

⁧وضعیت: انجام شده.⁩

⁧در صورت پروژه، ⁦Elasticsearch⁩ و ⁦OpenSearch⁩ به‌عنوان گزینه‌های مناسب برای داده متنی پیشنهاد شده‌اند. انتخاب این پروژه ⁦Elasticsearch⁩ است.⁩

⁧دلیل انتخاب ⁦Elasticsearch:⁩⁩

- ⁧توجیه کوتاه انتخاب ⁦Elasticsearch⁩ در ⁦`README.md`⁩ ثبت شده است.⁩

⁧گزینه مقایسه‌ای مناسب:⁩

- PostgreSQL Full-Text Search

⁧مقایسه کوتاه:⁩

- ⁧گزینهٔ مقایسه‌ای اصلی ⁦PostgreSQL Full-Text Search⁩ است؛ خلاصهٔ مقایسه در ⁦`README.md`⁩ آمده است.⁩

⁧خروجی مورد انتظار این مرحله:⁩

- ⁧فایل ⁦`docker-compose.yml`⁩ ساخته شد.⁩
- ⁧نسخه ⁦Elasticsearch 9.4.4⁩ ثبت شد.⁩
- ⁧منابع ⁦container⁩ شامل ⁦2GB memory⁩، ⁦2 CPU⁩، ⁦1GB heap⁩ و ⁦volume⁩ پایدار ثبت شد.⁩
- ⁧توضیح انتخاب تکنولوژی در همین مرحله ثبت شد.⁩
- ⁧⁦endpoint⁩ محلی ⁦`http://127.0.0.1:9200`⁩ برای مراحل بعد آماده شد.⁩

⁧معیار اتمام:⁩

- ⁧⁦Elasticsearch⁩ با ⁦Docker Compose⁩ بالا بیاید.⁩
- ⁧⁦health cluster⁩ قابل خواندن باشد.⁩
- ⁧نسخه‌ها و تنظیمات منابع در ⁦`elasticsearch/README.md`⁩ مستند شده باشند.⁩
- ⁧توجیه انتخاب ⁦Elasticsearch⁩ در مقایسه با حداقل یک گزینهٔ دیگر، مثلاً ⁦PostgreSQL Full-Text Search⁩ یا ⁦OpenSearch⁩، با معیارهایی مثل قابلیت ⁦full-text search⁩، ⁦fuzzy⁩، ⁦aggregation⁩، هزینه راه‌اندازی و تناسب با آزمایش‌های پروژه نوشته شده باشد.⁩

## ⁧مرحله ⁦3:⁩ طراحی ⁦index⁩ و ⁦mapping⁩ اولیه⁩

⁧وضعیت: انجام شده.⁩

⁧در این مرحله باید مشخص شود هر فیلد دیتاست پاک‌شده با چه نوعی در ⁦Elasticsearch⁩ ذخیره شود.⁩

⁧طرح اولیه ⁦mapping:⁩⁩

- ⁧⁦`paper_id`⁩: نوع ⁦`keyword`⁩ برای شناسه یکتا و ⁦lookup⁩ دقیق.⁩
- ⁧⁦`title`⁩: نوع ⁦`text`⁩ با ⁦analyzer⁩ استاندارد برای جست‌وجوی متنی.⁩
- ⁧⁦`abstract`⁩: نوع ⁦`text`⁩ با ⁦analyzer⁩ استاندارد برای جست‌وجوی متنی.⁩
- ⁧⁦`title_abstract`⁩: نوع ⁦`text`⁩ با ⁦analyzer⁩ استاندارد برای جست‌وجوی کلی روی ترکیب عنوان و چکیده.⁩
- ⁧⁦`authors`⁩: نوع ⁦`text`⁩ برای جست‌وجوی نام نویسنده در متن کامل فیلد.⁩
- ⁧⁦`categories`⁩: نوع ⁦`keyword`⁩ برای ⁦filter⁩ و ⁦aggregation.⁩⁩
- ⁧⁦`primary_category`⁩: نوع ⁦`keyword`⁩ برای ⁦filter⁩ و ⁦aggregation.⁩⁩
- ⁧⁦`year`⁩: نوع ⁦`integer`⁩ برای ⁦filter⁩، ⁦range⁩ و ⁦aggregation.⁩⁩
- ⁧⁦`update_date`⁩: نوع ⁦`date`⁩ با ⁦format⁩ دقیق ⁦`yyyy-MM-dd`⁩.⁩

⁧در این مرحله فقط ⁦index⁩ خط مبنا ساخته می‌شود. ساخت ⁦index⁩ بهینه‌شده باید تا مرحله ⁦9⁩ عقب بیفتد تا ابتدا ⁦baseline⁩ با طراحی ساده ثبت شود و مقایسه قبل/بعد آلوده نشود.⁩

- ⁧⁦`arxiv_papers_baseline`⁩: طراحی اولیه و ساده، بدون ⁦ngram⁩ و بدون ⁦multi-field⁩های بهینه‌سازی‌شده.⁩
- ⁧⁦`arxiv_papers_optimized`⁩: در این مرحله ساخته نمی‌شود؛ طراحی و ساخت آن متعلق به مرحله ⁦9⁩ است.⁩

⁧تنظیمات ثابت ⁦index⁩ خط مبنا در محیط محلی فعلی: ⁦`number_of_shards: 1`⁩ و ⁦`number_of_replicas: 0`⁩. چون محیط فعلی طبق ⁦`elasticsearch/README.md`⁩ تک‌نودی است، آزمایش واقعی ⁦replica⁩ در این محیط معتبر نیست؛ بررسی ⁦replica⁩ فقط با کلاستر چندنودی جدا قابل دفاع است.⁩

⁧نقشهٔ اجرای کامل مرحله ⁦3:⁩⁩

1. ⁧ساخت پوشه‌های لازم برای تنظیمات ⁦Elasticsearch⁩: ⁦`elasticsearch/mappings/`⁩ و در صورت نیاز ⁦`elasticsearch/settings/`⁩.⁩
2. ⁧نوشتن ⁦mapping⁩ خط مبنا در ⁦`elasticsearch/mappings/arxiv_papers_baseline.json`⁩ با ⁦`dynamic: "strict"`⁩ تا هیچ فیلد خارج از قرارداد دیتاست به‌صورت مبهم وارد ⁦index⁩ نشود.⁩
3. ⁧ثبت تنظیمات ثابت ⁦baseline index⁩ داخل همان فایل یا فایل تنظیمات همراه: ⁦`number_of_shards: 1`⁩ و ⁦`number_of_replicas: 0`⁩. در ⁦baseline⁩ هیچ ⁦ngram⁩، ⁦edge_ngram⁩، ⁦multi-field⁩ بهینه‌سازی‌شده یا ⁦analyzer⁩ اختصاصی تعریف نمی‌شود.⁩
4. ⁧استفاده از ⁦standard analyzer⁩ برای فیلدهای متنی ⁦`title`⁩، ⁦`abstract`⁩، ⁦`title_abstract`⁩ و ⁦`authors`⁩. فیلدهای ⁦`categories`⁩ و ⁦`primary_category`⁩ از نوع ⁦`keyword`⁩، فیلد ⁦`year`⁩ از نوع ⁦`integer`⁩ و فیلد ⁦`update_date`⁩ از نوع ⁦`date`⁩ با ⁦format⁩ دقیق ⁦`yyyy-MM-dd`⁩ هستند.⁩
5. ⁧نوشتن اسکریپت ساخت ⁦index⁩، مثلاً ⁦`scripts/create_index.py`⁩، با ورودی‌های ⁦`--index`⁩، ⁦`--mapping`⁩ و گزینهٔ صریح ⁦`--recreate`⁩. حذف ⁦index⁩ موجود فقط وقتی مجاز است که ⁦`--recreate`⁩ داده شود؛ اجرای پیش‌فرض نباید داده یا ⁦index⁩ موجود را حذف کند.⁩
6. ⁧اسکریپت بعد از ساخت ⁦index⁩ باید تنظیمات و ⁦mapping⁩ برگشتی از ⁦Elasticsearch⁩ را بخواند و بررسی کند که نام ⁦index⁩، تعداد ⁦shard/replica⁩، ⁦dynamic strict⁩ و نوع همهٔ ⁦9⁩ فیلد با فایل ⁦mapping⁩ برابر است.⁩
7. ⁧نوشتن تست‌های مرحله ⁦3⁩، مثلاً ⁦`tests/test_mapping.py`⁩، برای اعتبارسنجی ⁦JSON⁩ بودن فایل ⁦mapping⁩، وجود هر ⁦9⁩ فیلد، نبودن فیلد اضافه، نبود ⁦ngram/multi-field⁩ در ⁦baseline⁩، و درست‌بودن ⁦shard/replica⁩.⁩
8. ⁧ثبت دستور بازتولید مرحله ⁦3⁩ در مستندات، شامل دستور ساخت ⁦index⁩ و دستور بررسی ⁦mapping⁩ از ⁦Elasticsearch.⁩⁩
9. ⁧در پایان این مرحله فقط ⁦`arxiv_papers_baseline`⁩ ساخته می‌شود. هیچ داده‌ای وارد ⁦index⁩ نمی‌شود؛ ورود داده متعلق به مرحله ⁦4⁩ است. ⁦`arxiv_papers_optimized`⁩ و اجرای ⁦benchmark⁩ نیز خارج از مرحله ⁦3⁩ هستند.⁩

⁧تعریف دقیق ⁦contain search⁩ و آزمایش زیررشته:⁩

- ⁧⁦contain⁩ اجباری پروژه به معنی وجود واژه، عبارت متوالی، یا شرط‌های مثبت/منفی روی توکن‌هاست؛ این بخش با جست‌وجوی زیررشتهٔ دلخواه یکی نیست و نباید با آن مخلوط شود.⁩
- ⁧وجود واژه با ⁦`match`⁩ داخل ⁦`bool.must`⁩ پیاده‌سازی می‌شود.⁩
- ⁧وجود عبارت متوالی با ⁦`match_phrase`⁩ روی ⁦`title`⁩ و ⁦`abstract`⁩ پیاده‌سازی می‌شود.⁩
- ⁧شرط «شامل ⁦`indexing`⁩ ولی نه ⁦`blockchain`⁩» با ترکیب ⁦`bool.must`⁩ و ⁦`bool.must_not`⁩ پیاده‌سازی می‌شود.⁩
- ⁧زیررشتهٔ واقعی یک آزمایش جداست، نه بخشی از ⁦contain⁩ اجباری. استفاده از ⁦`wildcard`⁩ روی فیلد معمولی ⁦`text`⁩ معیار قابل اتکایی برای زیررشتهٔ متن اصلی نیست.⁩
- ⁧برای آزمایش جداگانهٔ زیررشته باید ورودی و معنای منطقی در دو نسخه یکسان باشد؛ مثلاً «وجود زیررشتهٔ دقیق ⁦`transact`⁩ در ⁦`title_abstract`⁩». یک نسخه با راهکار ⁦wildcard⁩ روی فیلد مناسب و نسخهٔ دیگر با ⁦ngram⁩ اجرا می‌شود. این آزمایش باید از نتایج ⁦contain⁩ واژه/عبارت جدا گزارش شود تا تعداد نتیجه و ⁦latency⁩ قابل مقایسه بماند.⁩
- ⁧آزمایش ⁦wildcard vs ngram⁩ باید روی دو ⁦index⁩ آزمایشی جدا انجام شود، نه روی ⁦index⁩های اصلی ⁦baseline/optimized⁩: ⁦`arxiv_papers_substring_wildcard`⁩ و ⁦`arxiv_papers_substring_ngram`⁩. هر دو از همان دیتاست پاک‌شده و همان پروتکل ورود داده استفاده می‌کنند.⁩
- ⁧در ⁦`arxiv_papers_substring_wildcard`⁩ فیلد کمکی ⁦`title_abstract_substring`⁩ با نوع ⁦`wildcard`⁩ ساخته می‌شود و مقدار آن از ⁦`title_abstract`⁩ پاک‌شده، با ⁦Unicode NFC⁩، ⁦lowercase⁩ و فاصلهٔ نرمال‌شده تولید می‌شود. ⁦query⁩ نمونه با الگوی یکسان مثل ⁦`*transact*`⁩ و روی همین فیلد اجرا می‌شود.⁩
- ⁧در ⁦`arxiv_papers_substring_ngram`⁩ همان فیلد کمکی ⁦`title_abstract_substring`⁩ با نوع ⁦`text`⁩ و ⁦analyzer⁩ اختصاصی ساخته می‌شود: ⁦standard tokenizer⁩، ⁦lowercase filter⁩ و ⁦ngram token filter⁩ با ⁦`min_gram: 3`⁩ و ⁦`max_gram: 20`⁩. ⁦search_analyzer⁩ باید ⁦standard + lowercase⁩ باشد تا ورودی منطقی همان زیررشتهٔ کاربر باقی بماند.⁩
- ⁧برای منصفانه‌بودن، هر دو آزمایش باید ⁦`track_total_hits: true`⁩، ⁦size⁩ یکسان، ⁦query⁩های زیررشته‌ای یکسان، ⁦warm-up⁩ یکسان و پروتکل ⁦benchmark⁩ یکسان داشته باشند. در گزارش باید تعداد نتیجه، ⁦latency/P95/throughput⁩، اندازه ⁦index⁩ و زمان ⁦ingestion⁩ هر دو ⁦index⁩ جدا ثبت شود.⁩
- ⁧در ⁦optimized⁩، برای زیررشتهٔ واقعی باید از ⁦ngram⁩ یا راهکار مشخص دیگری استفاده شود. ⁦edge_ngram⁩ فقط ابتدای توکن را پوشش می‌دهد و برای زیررشتهٔ دلخواه کافی نیست؛ بنابراین فقط در صورت نیاز به ⁦prefix/search-as-you-type⁩ مجاز است، نه به‌عنوان راهکار عمومی زیررشته.⁩
- ⁧تفاوت اصلی ⁦wildcard-based substring⁩ و ⁦ngram-based substring⁩ باید در آزمایش جدا اندازه‌گیری شود: ⁦wildcard⁩ معمولاً ⁦index⁩ کوچک‌تر دارد اما ممکن است کندتر باشد؛ ⁦ngram⁩ معمولاً زیررشته را سریع‌تر و قابل کنترل‌تر می‌کند، اما اندازه ⁦index⁩ و هزینه ⁦ingestion⁩ را بالا می‌برد.⁩
- ⁧هر ⁦query contain⁩ یا زیررشته باید در خروجی ثبت کند از کدام فیلد، ⁦analyzer⁩ و معنای منطقی استفاده کرده است تا نتیجه قابل بازتولید باشد.⁩

⁧خروجی مورد انتظار:⁩

- ⁧فایل ⁦mapping⁩ خط مبنا، مثلاً ⁦`elasticsearch/mappings/arxiv_papers_baseline.json`⁩⁩
- ⁧اسکریپت ساخت ⁦index⁩، مثلاً ⁦`scripts/create_index.py`⁩⁩
- ⁧تست‌های ⁦mapping⁩ و ساختار ⁦index⁩، مثلاً ⁦`tests/test_mapping.py`⁩⁩
- ⁧مستند توضیح نوع هر فیلد و دستور بازتولید مرحله ⁦3⁩⁩

⁧معیار اتمام:⁩

- ⁧⁦index⁩ خط مبنا با ⁦mapping⁩ مشخص و نام ثابت ⁦`arxiv_papers_baseline`⁩ ساخته شود.⁩
- ⁧⁦mapping⁩ قابل بازتولید باشد.⁩
- ⁧هیچ ⁦dynamic mapping⁩ مبهمی برای فیلدهای اصلی باقی نماند.⁩
- ⁧تست‌های ⁦mapping⁩ پاس شوند و ثابت کنند ⁦baseline⁩ فاقد ⁦ngram⁩، ⁦edge_ngram⁩، ⁦multi-field⁩ بهینه‌سازی‌شده و فیلد اضافه است.⁩
- ⁧اسکریپت ساخت ⁦index⁩ بدون ⁦`--recreate`⁩ هیچ ⁦index⁩ موجودی را حذف نکند.⁩
- ⁧مرحله بدون ورود داده، بدون ساخت ⁦optimized index⁩ و بدون اجرای ⁦benchmark⁩ تمام شود.⁩

⁧نتیجهٔ اجرای واقعی مرحله ⁦3:⁩⁩

- ⁧فایل ⁦`elasticsearch/mappings/arxiv_papers_baseline.json`⁩ ساخته شد.⁩
- ⁧اسکریپت ⁦`scripts/create_index.py`⁩ ساخته شد و اجرای پیش‌فرض آن فقط ⁦index⁩ موجود را اعتبارسنجی می‌کند؛ حذف و ساخت مجدد فقط با ⁦`--recreate`⁩ انجام می‌شود.⁩
- ⁧تست ⁦`tests/test_mapping.py`⁩ ساخته شد.⁩
- ⁧⁦index⁩ واقعی ⁦`arxiv_papers_baseline`⁩ در ⁦Elasticsearch 9.4.4⁩ ساخته و دوباره اعتبارسنجی شد.⁩
- ⁧هش ⁦SHA-256⁩ فایل ⁦mapping⁩ برابر است با ⁦`c0060d085c1113f2534e9e2638e10b801df4d41dae5b2ca3690ebec27f0b951f`⁩.⁩
- ⁧تنظیمات واقعی ⁦index⁩ تأیید شد: ⁦`number_of_shards: 1`⁩، ⁦`number_of_replicas: 0`⁩ و ⁦`dynamic: strict`⁩.⁩
- ⁧تعداد سندهای ⁦`arxiv_papers_baseline`⁩ پس از مرحله ⁦3⁩ برابر ⁦0⁩ است؛ داده هنوز وارد نشده و ورود داده متعلق به مرحله ⁦4⁩ است.⁩
- ⁧⁦`arxiv_papers_optimized`⁩ ساخته نشده و هیچ ⁦benchmark⁩ اجرا نشده است.⁩
- ⁧همهٔ تست‌های فعلی پروژه با ⁦`conda run --no-capture-output -n ai_agent python -m pytest -q`⁩ پاس شدند: ⁦71 passed⁩ و ⁦2 subtests passed⁩.⁩

## ⁧مرحله ⁦4:⁩ ورود دسته‌ای داده به پایگاه داده⁩

⁧وضعیت: انجام شده و در محیط زندهٔ فعلی هم بارگذاری شده است.⁩

⁧در این مرحله دیتاست پاک‌شده باید با ⁦bulk insert⁩ فقط وارد ⁦`arxiv_papers_baseline`⁩ شود. ورود تک‌به‌تک رکوردها قابل قبول نیست. ورود داده به ⁦`arxiv_papers_optimized`⁩ متعلق به مرحله ⁦9⁩ است.⁩

⁧پروتکل ورود داده برای ساخت ⁦index⁩های اصلی مقایسهٔ جست‌وجو باید ثابت بماند. آزمایش‌های تغییر ⁦batch size⁩ یا ⁦refresh_interval⁩ نباید روی ⁦`arxiv_papers_baseline`⁩ یا ⁦`arxiv_papers_optimized`⁩ اصلی انجام شوند؛ این آزمایش‌ها باید روی ⁦index⁩های موقت جدا، مثل ⁦`arxiv_papers_ingest_test_*`⁩، اجرا شوند تا اثر ⁦ingestion⁩ با اثر ⁦mapping/analyzer⁩ مخلوط نشود.⁩

⁧کارهای لازم:⁩

- ⁧خواندن ⁦`dataset/arxiv_project_sample_50k_cleaned.jsonl`⁩ به‌صورت ⁦streaming.⁩⁩
- ⁧ساخت ⁦bulk request⁩ها با ⁦batch size⁩ کنترل‌شده.⁩
- ⁧استفاده از ⁦`paper_id`⁩ به‌عنوان ⁦`_id`⁩ سند برای جلوگیری از ⁦duplicate.⁩⁩
- ⁧ثبت زمان شروع و پایان ⁦ingestion.⁩⁩
- ⁧ثبت تعداد سندهای واردشده.⁩
- ⁧ثبت تعداد خطاهای ⁦bulk.⁩⁩
- ⁧خواندن اندازه ⁦index⁩ بعد از ورود.⁩
- ⁧ثبت اینکه این گزارش مربوط به ⁦ingestion benchmark⁩ خط مبناست، نه ⁦search benchmark⁩.⁩

⁧خروجی مورد انتظار:⁩

- ⁧اسکریپت ⁦bulk ingestion⁩، مثلاً ⁦`scripts/index_dataset.py`⁩⁩
- ⁧گزارش ⁦ingestion⁩ شامل ⁦document count⁩، زمان ورود و اندازه ⁦index⁩⁩
- ⁧دستور بازتولید ⁦ingestion⁩⁩

⁧معیار اتمام:⁩

- ⁧تعداد اسناد ⁦index⁩ برابر ⁦50⁩,⁦000⁩ شود.⁩
- ⁧خطای ⁦bulk⁩ صفر یا کاملاً توضیح داده‌شده باشد.⁩
- ⁧زمان ورود و اندازه ⁦index⁩ ثبت شود.⁩

⁧نتیجهٔ ثبت‌شدهٔ مرحله ⁦4:⁩⁩

- ⁧اسکریپت ⁦`scripts/index_dataset.py`⁩ اضافه شده است.⁩
- ⁧گزارش ⁦`results/ingestion_baseline.json`⁩ با وضعیت ⁦`passed`⁩ موجود است.⁩
- ⁧ورودی گزارش ⁦`dataset/arxiv_project_sample_50k_cleaned.jsonl`⁩ با هش ⁦`86b9febd7fc85d1b9c97377b36db525391854c134430b85e30c87a1dc18f2ad6`⁩ است.⁩
- ⁧طبق گزارش، ⁦50,000⁩ سند در ⁦100⁩ دستهٔ ⁦500⁩تایی وارد شده‌اند، خطای ⁦bulk⁩ برابر ⁦0⁩ بوده، مدت ورود ⁦7.720882s⁩ و ⁦throughput⁩ حدود ⁦6475.944 docs/s⁩ ثبت شده است.⁩
- ⁧اندازهٔ ⁦index⁩ پس از اجرای ثبت‌شده ⁦103,115,180 bytes⁩ بوده است.⁩
- ⁧نکتهٔ عملی: در بازبینی نهایی ⁦2026-08-08⁩، ⁦index⁩ زندهٔ ⁦`arxiv_papers_baseline`⁩ دوباره ساخته و با ⁦50,000⁩ سند بارگذاری شد. ⁦count⁩ مستقیم ⁦Elasticsearch⁩ و ⁦smoke test⁩ این وضعیت را تأیید کردند.⁩

## ⁧مرحله ⁦5:⁩ پیاده‌سازی ⁦query⁩های پایه، ابزار اندازه‌گیری و خط مبنای کارایی⁩

⁧وضعیت: انجام شده برای خط مبنای تک‌کلاینت.⁩

⁧در این مرحله سه روش اصلی جست‌وجو باید جداگانه پیاده‌سازی شوند:⁩

- ⁧⁦`keyword search`⁩: جست‌وجوی واژه یا عبارت ورودی کاربر، مثلاً با ⁦`match`⁩، ⁦`multi_match`⁩ یا ⁦`match_phrase`⁩.⁩
- ⁧⁦`contain search`⁩: بررسی وجود واژه، عبارت متوالی یا شرط‌های مثبت/منفی طبق تعریف مرحله ⁦3⁩؛ وجود واژه با ⁦`match`⁩، عبارت متوالی با ⁦`match_phrase`⁩، و شرط‌های مثبت/منفی با ⁦`bool.must`⁩ و ⁦`bool.must_not`⁩. زیررشتهٔ واقعی در آزمایش جداگانهٔ ⁦wildcard vs ngram⁩ بررسی می‌شود و با ⁦contain⁩ اجباری مخلوط نمی‌شود.⁩
- ⁧⁦`fuzzy search`⁩: جست‌وجوی مقاوم به غلط تایپی، با هزینه اجرایی بیشتر.⁩

⁧قبل از ثبت ⁦baseline⁩، ابزار اندازه‌گیری کامل باید آماده باشد. بنابراین اسکریپت اجرای ⁦query⁩ها باید ⁦latency⁩ تک‌تک درخواست‌ها، تعداد درخواست موفق، تعداد خطا، زمان شروع و پایان آزمایش، متریک‌های ⁦Elasticsearch/Docker⁩، اندازه ⁦index⁩ و تعداد سندها را در فایل خروجی ذخیره کند. اگر ابزار اندازه‌گیری بعد از ⁦baseline⁩ نوشته شود، مقایسه قبل/بعد قابل اعتماد نیست. مرحله ⁦7⁩ فقط مسیر تجمیع، اعتبارسنجی و نمودارگیری را با خروجی‌های اولیه تست می‌کند؛ تولید نهایی جدول‌ها و نمودارهای کامل بعد از مرحله ⁦9⁩ انجام می‌شود.⁩

⁧⁦query⁩های اجباری:⁩

- ⁧⁦Keyword:⁩ مقاله‌های مربوط به ⁦`query optimization`⁩.⁩
- ⁧⁦Keyword:⁩ مقاله‌های مربوط به ⁦`distributed database`⁩.⁩
- ⁧⁦Keyword:⁩ مقاله‌های مربوط به ⁦`learned indexes`⁩ با فیلتر دسته‌بندی ⁦`cs.DB`⁩.⁩
- ⁧⁦Contain:⁩ مقاله‌هایی که عبارت یا الگوی ⁦`database systems`⁩ در عنوان یا چکیده دارند.⁩
- ⁧⁦Contain:⁩ مقاله‌هایی که شامل ⁦`indexing`⁩ باشند ولی شامل ⁦`blockchain`⁩ نباشند.⁩
- ⁧⁦Contain:⁩ مقاله‌هایی که شامل یکی از عبارت‌های ⁦`query processing`⁩، ⁦`storage engine`⁩ یا ⁦`transaction processing`⁩ باشند.⁩
- ⁧⁦Fuzzy:⁩ جست‌وجوی ⁦`databse optimiztion`⁩.⁩
- ⁧⁦Fuzzy:⁩ جست‌وجوی ⁦`transacton procesing`⁩.⁩
- ⁧⁦Aggregation/Analysis:⁩ برای یک ⁦query⁩ دلخواه، توزیع نتایج بر اساس سال و دسته‌بندی علمی.⁩

⁧برای هر ⁦query⁩ باید ثبت شود:⁩

- ⁧تعداد نتایج.⁩
- ⁧میانگین ⁦latency.⁩⁩
- P95 latency.
- throughput.
- ⁧متریک‌های مرتبط سیستم.⁩

⁧قرارداد هر ⁦query⁩ باید قبل از اجرا در یک فایل ماشین‌خوان ثبت شود؛ مثلاً ⁦`queries/search_queries.json`⁩. بخش ثابت قرارداد، نیت منطقی جست‌وجو و ورودی کاربر است؛ مثلاً «پیدا کردن مقاله‌های مرتبط با ⁦`database systems`⁩» یا «شامل ⁦`indexing`⁩ و فاقد ⁦`blockchain`⁩». پیاده‌سازی اجرایی می‌تواند برای ⁦baseline⁩ و ⁦optimized⁩ متفاوت باشد، چون مقایسهٔ ⁦match/wildcard⁩ با ⁦ngram⁩ یا ⁦analyzer⁩های متفاوت دقیقاً بخشی از آزمایش است. بنابراین برای هر نسخه باید ⁦DSL⁩، فیلدها، ⁦boost⁩ها، ⁦operator⁩، ⁦slop⁩، ⁦fuzziness⁩، ⁦prefix_length⁩، ⁦size⁩ خروجی، ⁦`track_total_hits`⁩، پارامترهای ⁦ngram⁩، استفاده از ⁦filter context⁩ و نام ⁦index⁩ هدف جداگانه ثبت شود. فیلترهای ⁦`year`⁩ و ⁦`categories`⁩ در همهٔ نسخه‌های قابل مقایسه باید از ابتدا در ⁦filter context⁩ باشند و به‌عنوان بهینه‌سازی مرحله ⁦9⁩ شمرده نشوند. نتیجهٔ هر آزمایش باید مسیر یا ⁦hash⁩ فایل تعریف منطقی ⁦query⁩ و تعریف اجرایی همان نسخه را کنار ⁦mapping hash⁩ ذخیره کند.⁩

⁧برای تعداد دقیق نتایج، همهٔ ⁦search request⁩های قابل مقایسه باید ⁦`track_total_hits: true`⁩ داشته باشند. این گزینه هزینهٔ اجرایی دارد، اما چون تعداد نتایج یکی از خروجی‌های گزارش است، باید در همهٔ حالت‌های ⁦baseline⁩، ⁦optimized⁩ و ⁦hybrid⁩ ثابت بماند. اگر برای یک آزمایش خاص غیرفعال شود، آن آزمایش نباید با حالت‌های دارای شمارش دقیق مقایسه شود.⁩

⁧پروتکل دقیق ⁦benchmark:⁩⁩

- ⁧هر ⁦query⁩ قبل از اندازه‌گیری اصلی ⁦5⁩ بار ⁦warm-up⁩ اجرا می‌شود و نتایج ⁦warm-up⁩ در محاسبات نهایی وارد نمی‌شود.⁩
- ⁧برای هر ⁦query⁩ در حالت تک‌کلاینت حداقل ⁦30⁩ اجرای اندازه‌گیری‌شده انجام می‌شود.⁩
- ⁧برای تست‌های زمان‌محور، هر سناریو حداقل ⁦60⁩ ثانیه اجرا می‌شود؛ اگر تعداد نمونه ⁦latency⁩ کمتر از ⁦100⁩ شد، مدت آزمایش تا رسیدن به حداقل ⁦100⁩ نمونه افزایش می‌یابد.⁩
- ⁧⁦latency⁩ از سمت ⁦client⁩ و به‌صورت ⁦end-to-end⁩ اندازه‌گیری می‌شود، یعنی از لحظه ارسال درخواست تا دریافت کامل پاسخ.⁩
- ⁧⁦P95⁩ از روی ⁦latency⁩های اندازه‌گیری‌شده همان سناریو محاسبه می‌شود: ⁦latency⁩ها صعودی مرتب می‌شوند و مقدار صدک ⁦95⁩ گزارش می‌شود.⁩
- ⁧⁦throughput⁩ ساده برابر است با تعداد ⁦query⁩های موفق تقسیم بر مدت اندازه‌گیری اصلی، بدون احتساب ⁦warm-up.⁩⁩
- ⁧برای معماری ترکیبی، دو ⁦throughput⁩ جدا ثبت می‌شود: ⁦user-facing throughput⁩ یعنی تعداد جست‌وجوی نهایی کاربر در ثانیه، و ⁦internal Elasticsearch request throughput⁩ یعنی تعداد درخواست داخلی ارسال‌شده به ⁦Elasticsearch⁩ در ثانیه.⁩
- ⁧⁦error rate⁩ برابر است با تعداد ⁦query⁩های ناموفق تقسیم بر کل ⁦query⁩های ارسال‌شده.⁩
- ⁧قبل از هر ⁦benchmark⁩، نسخه ⁦Elasticsearch⁩، نام ⁦index⁩، ⁦mapping hash⁩ یا فایل ⁦mapping⁩، مسیر یا ⁦hash⁩ تعریف منطقی و اجرایی ⁦query⁩، تعداد ⁦shard/replica⁩، ⁦heap⁩، محدودیت ⁦CPU/memory⁩، ⁦batch size⁩ و اندازه ⁦index⁩ ثبت می‌شود.⁩
- ⁧مصرف ⁦CPU⁩ و ⁦memory⁩ از ⁦Docker stats/API⁩ و در صورت نیاز از ⁦Elasticsearch nodes stats API⁩ با فاصله نمونه‌برداری ثابت، مثلاً ⁦1s⁩، خوانده می‌شود. گزارش هر سناریو باید حداقل میانگین و بیشینهٔ ⁦CPU/memory⁩ را ذخیره کند.⁩
- ⁧برای کاهش اثر ⁦cache⁩، ترتیب ⁦query⁩ها با ⁦seed⁩ ثابت ⁦shuffle⁩ می‌شود و ⁦seed⁩ در خروجی ثبت می‌شود. ⁦cache⁩ عمداً پاک نمی‌شود مگر برای آزمایش جداگانه؛ چون هدف اصلی مقایسه در شرایط عملیاتی پایدار است.⁩
- ⁧محیط اجرای ⁦baseline⁩ و ⁦optimized⁩ باید ثابت بماند: همان ماشین، همان ⁦Docker Compose⁩، همان محدودیت منابع، همان ⁦dataset⁩، همان تعداد کلاینت و همان مدت آزمایش.⁩

⁧خروجی مورد انتظار:⁩

- ⁧کد اجرای ⁦query⁩ها، مثلاً ⁦`scripts/search_queries.py`⁩⁩
- ⁧رابط گرافیکی اختیاری دمو، ⁦`scripts/search_ui.py`⁩، برای جست‌وجوی دستی روی ⁦baseline/optimized⁩. این ابزار جزو معیار رسمی پروژه نیست.⁩
- ⁧کد مشترک اندازه‌گیری و ذخیره متریک‌ها، مثلاً ⁦`scripts/benchmark.py`⁩⁩
- ⁧خروجی ⁦JSON⁩ یا ⁦CSV⁩ از نتایج ⁦query⁩ها⁩
- ⁧جدول ⁦baseline performance⁩⁩
- ⁧حداقل یک نمودار ⁦matplotlib⁩⁩

⁧معیار اتمام:⁩

- ⁧هر ⁦query⁩ اجباری قابل اجرای مجدد باشد.⁩
- ⁧نتایج و متریک‌ها ذخیره شوند.⁩
- ⁧خط مبنای ⁦performance⁩ قبل از بهینه‌سازی مشخص باشد.⁩

⁧نتیجهٔ ثبت‌شدهٔ مرحله ⁦5:⁩⁩

- ⁧فایل قرارداد ⁦query⁩ها در ⁦`queries/search_queries.json`⁩ موجود است.⁩
- ⁧اسکریپت‌های ⁦`scripts/search_queries.py`⁩ و ⁦`scripts/benchmark.py`⁩ اضافه شده‌اند.⁩
- ⁧گزارش ⁦`results/search_baseline.json`⁩ با وضعیت ⁦`passed`⁩، ⁦`protocol_compliant: true`⁩ و ⁦`resource_metrics_complete: true`⁩ موجود است.⁩
- ⁧خروجی‌های ⁦`results/search_baseline.csv`⁩ و ⁦`results/search_baseline_latency.png`⁩ ساخته شده‌اند.⁩
- ⁧گزارش شامل ⁦9⁩ سناریوی ⁦keyword/contain/fuzzy/aggregation⁩، ⁦5⁩ اجرای ⁦warm-up⁩، ⁦30⁩ اجرای اندازه‌گیری‌شده برای هر ⁦query⁩، ⁦track_total_hits: true⁩، متریک‌های ⁦latency/P95/throughput/error/resource⁩ و هش‌های ⁦dataset/mapping/query contract⁩ است.⁩

## ⁧مرحله ⁦6:⁩ معماری ترکیبی جست‌وجو⁩

⁧وضعیت: انجام شده برای معماری ترکیبی خط مبنا.⁩

⁧در این مرحله باید معماری‌ای ساخته شود که ⁦keyword⁩، ⁦contain⁩ و ⁦fuzzy⁩ را ترکیب کند. هدف این است که کیفیت نتایج خوب باشد، اما ⁦fuzzy⁩ فقط وقتی اجرا شود که لازم است، چون از نظر ⁦performance⁩ سنگین‌تر است.⁩

⁧معماری پیشنهادی:⁩

1. ⁧اجرای ⁦keyword search⁩ و ⁦contain search⁩ به‌صورت موازی.⁩
2. ⁧ترکیب نتایج دو روش بعد از پایان هر دو درخواست.⁩
3. ⁧حذف ⁦duplicate⁩ها بر اساس ⁦`paper_id`⁩.⁩
4. ⁧محاسبه ⁦score⁩ نهایی با ⁦rank fusion⁩، نه با جمع مستقیم ⁦score⁩ خام ⁦Elasticsearch.⁩⁩
5. ⁧اگر تعداد نتیجه کافی نبود یا ⁦score⁩ نتایج از آستانه پایین‌تر بود، اجرای ⁦fuzzy search.⁩⁩
6. ⁧اضافه‌کردن نتایج ⁦fuzzy⁩ به خروجی نهایی با وزن کنترل‌شده.⁩

⁧فرمول پیشنهادی ⁦score⁩ نهایی:⁩

```text
final_score(doc) =
  1.00 * RRF(keyword_rank) +
  0.90 * RRF(contain_rank) +
  0.65 * RRF(fuzzy_rank)

RRF(rank) = 1 / (60 + rank)
```

⁧اگر مقاله‌ای در یک روش پیدا نشود، سهم همان روش صفر است. اگر مقاله در چند روش پیدا شود، امتیازهای ⁦rank-based⁩ آن جمع می‌شوند. این روش عمداً از ⁦score⁩ خام ⁦Elasticsearch⁩ استفاده نمی‌کند، چون ⁦score⁩ خام ⁦`match`⁩، ⁦`wildcard`⁩، ⁦ngram⁩ و ⁦fuzzy⁩ مستقیماً قابل مقایسه نیست.⁩

⁧آستانه اجرای ⁦fuzzy:⁩⁩

- ⁧اگر بعد از ⁦merge⁩ نتایج ⁦keyword⁩ و ⁦contain⁩ کمتر از ⁦20⁩ نتیجه یکتا وجود داشت، ⁦fuzzy⁩ اجرا شود.⁩
- ⁧اگر هیچ‌کدام از ⁦10⁩ نتیجه اول همزمان از هر دو روش ⁦keyword⁩ و ⁦contain⁩ نیامده بودند، ⁦fuzzy⁩ اجرا شود.⁩
- ⁧اگر ⁦query⁩ در فهرست سناریوهای غلط تایپی اجباری باشد، ⁦fuzzy⁩ اجرا شود. در نسخهٔ اولیه همین قاعده همراه با دو شرط بالا کافی است. هیچ شرطی نباید برای تصمیم اجرای ⁦fuzzy⁩ نیازمند اجرای قبلی ⁦fuzzy⁩ باشد؛ بنابراین تشخیص‌هایی مثل «با ⁦`fuzziness=AUTO`⁩ نتیجه می‌گیرد» معیار تصمیم نیستند و فقط بعد از اجرای مجاز ⁦fuzzy⁩ قابل گزارش‌اند.⁩
- ⁧در غیر این صورت ⁦fuzzy⁩ اجرا نشود و ⁦latency⁩ پایین‌تر حفظ شود.⁩

⁧وزن‌های ⁦RRF⁩ و آستانه‌های بالا قبل از اجرای آزمایش‌ها به‌عنوان فرض اولیه ثابت می‌شوند. اگر قرار باشد تنظیم شوند، باید با مجموعه‌ای جدا از سه ⁦query⁩ مقایسه‌ای انجام شود تا نتایج ⁦`learned indexes`⁩، ⁦`query optimization`⁩ و ⁦`databse optimiztion`⁩ دچار تنظیم روی دادهٔ آزمون نشوند.⁩

⁧مواردی که باید صریحاً مستند شوند:⁩

- ⁧چرا این معماری انتخاب شده است.⁩
- ⁧⁦fuzzy search⁩ دقیقاً در چه شرایطی اجرا می‌شود.⁩
- ⁧نتایج چند روش چگونه ⁦merge⁩ و ⁦rank⁩ می‌شوند.⁩
- ⁧اگر یک مقاله در چند روش پیدا شود، ⁦score⁩ نهایی چطور حساب می‌شود.⁩
- ⁧وزن‌های روش‌ها و مقدار ثابت ⁦`k=60`⁩ در ⁦RRF⁩ چرا انتخاب شده‌اند و آیا در آزمایش‌ها تغییر کرده‌اند یا نه.⁩
- ⁧این معماری نسبت به اجرای همیشگی ⁦fuzzy⁩ چه مزایا و معایبی دارد.⁩

⁧⁦query⁩های مقایسه‌ای:⁩

- `learned indexes`
- `query optimization`
- `databse optimiztion`

⁧برای هر عبارت بالا باید چهار خروجی مقایسه شود:⁩

- keyword-only
- contain-only
- fuzzy-only
- hybrid

⁧معیارهای مقایسه:⁩

- ⁧تعداد نتایج.⁩
- ⁧کیفیت نتایج.⁩
- latency.
- P95 latency.
- ⁦user-facing throughput⁩.
- ⁦internal Elasticsearch request throughput⁩.
- ⁧⁦Precision@10⁩ بر اساس بررسی دستی دقیقاً ⁦10⁩ نتیجهٔ اول هر حالت، اگر حداقل ⁦10⁩ نتیجه وجود داشته باشد؛ در غیر این صورت تمام نتایج موجود همان حالت بررسی می‌شود.⁩

⁧پروتکل بررسی دستی کیفیت:⁩

- ⁧برای ⁦3 query⁩ مقایسه‌ای و ⁦4⁩ روش، در هر حالت دقیقاً ⁦10⁩ نتیجهٔ اول قضاوت می‌شود اگر حداقل ⁦10⁩ نتیجه وجود داشته باشد؛ اگر کمتر از ⁦10⁩ نتیجه وجود داشت، تمام نتایج موجود قضاوت می‌شوند و تعداد نتیجهٔ برگشتی ثبت می‌شود. بنابراین تعداد قضاوت‌ها در حالت کامل ⁦120⁩ است، اما اگر بعضی روش‌ها کمتر از ⁦10⁩ نتیجه بدهند کمتر از ⁦120⁩ خواهد بود.⁩
- ⁧هر قضاوت باید یکی از دو مقدار ⁦`relevant`⁩ یا ⁦`not_relevant`⁩ داشته باشد.⁩
- ⁧معیار مرتبط‌بودن این است که عنوان یا چکیده واقعاً درباره مفهوم ⁦query⁩ باشد، نه فقط شامل یک کلمه اتفاقی از ⁦query.⁩⁩
- ⁧برای ⁦query⁩ دارای غلط تایپی مثل ⁦`databse optimiztion`⁩، مرتبط‌بودن نسبت به مفهوم درست ⁦`database optimization`⁩ سنجیده می‌شود.⁩
- ⁧خروجی قضاوت‌ها باید در فایل داده ذخیره شود، مثلاً ⁦`results/relevance_judgments.csv`⁩ با ستون‌های ⁦`query`⁩, ⁦`method`⁩, ⁦`rank`⁩, ⁦`paper_id`⁩, ⁦`title`⁩, ⁦`judgment`⁩, ⁦`notes`⁩. فایل خلاصهٔ کیفیت هم باید ⁦`returned_count`⁩، ⁦`judged_count`⁩ و ⁦`missing_ranks`⁩ را نگه دارد.⁩
- ⁧متریک اصلی کیفیت ⁦`Precision@10 = relevant_count / 10`⁩ است و برای هر ⁦query/method⁩ جدا گزارش می‌شود. رتبه‌های خالی در مخرج ⁦10⁩ باقی می‌مانند تا روشی که کمتر از ⁦10⁩ نتیجه می‌دهد به‌درستی جریمه شود؛ در کنار آن ⁦`Precision@returned = relevant_count / returned_count`⁩ هم برای تفسیر تکمیلی ثبت می‌شود.⁩

⁧خروجی مورد انتظار:⁩

- ⁧کد معماری ترکیبی، مثلاً ⁦`scripts/hybrid_search.py`⁩⁩
- ⁧فایل نتایج مقایسه‌ای⁩
- ⁧جدول ⁦quality⁩ و ⁦performance⁩⁩
- ⁧توضیح فرمول ⁦score⁩ نهایی⁩

⁧معیار اتمام:⁩

- ⁧معماری ترکیبی برای سه ⁦query⁩ مقایسه‌ای اجرا شود.⁩
- ⁧خروجی آن با سه روش پایه مقایسه شود.⁩
- ⁧تحلیل کیفیت و کارایی قابل دفاع باشد.⁩

⁧نتیجهٔ ثبت‌شدهٔ مرحله ⁦6:⁩⁩

- ⁧فایل قرارداد ⁦`queries/hybrid_comparison_queries.json`⁩ موجود است.⁩
- ⁧اسکریپت‌های ⁦`scripts/hybrid_search.py`⁩ و ⁦`scripts/evaluate_relevance.py`⁩ اضافه شده‌اند.⁩
- ⁧گزارش ⁦`results/hybrid_comparison.json`⁩ با وضعیت ⁦`passed`⁩، ⁦`protocol_compliant: true`⁩ و ⁦`resource_metrics_complete: true`⁩ موجود است.⁩
- ⁧خروجی‌های ⁦`results/hybrid_performance.csv`⁩، ⁦`results/relevance_judgments.csv`⁩، ⁦`results/hybrid_quality.csv`⁩، ⁦`results/hybrid_quality.json`⁩ و ⁦`results/stage6_analysis.md`⁩ ساخته شده‌اند.⁩
- ⁧معماری ثبت‌شده از ⁦weighted RRF⁩ با ⁦`k=60`⁩ و وزن‌های ⁦keyword=1.00⁩، ⁦contain=0.90⁩ و ⁦fuzzy=0.65⁩ استفاده می‌کند. در حالت ⁦hybrid⁩، ⁦keyword⁩ و ⁦contain⁩ ابتدا موازی اجرا می‌شوند و ⁦fuzzy⁩ فقط طبق آستانهٔ ثبت‌شده اجرا می‌شود.⁩
- ⁧ارزیابی دستی کیفیت ⁦101⁩ قضاوت دارد؛ کمتر از ⁦120⁩ چون بعضی حالت‌ها کمتر از ⁦10⁩ نتیجه برگردانده‌اند. محاسبهٔ ⁦Precision@10⁩ با جریمهٔ رتبه‌های خالی ثبت شده است.⁩

## ⁧مرحله ⁦7:⁩ تولید جدول و نمودار متریک‌ها⁩

⁧وضعیت: انجام شده برای تجمیع متریک‌های موجود؛ گزارش نهایی بعد از مرحله ⁦9⁩ باید دوباره تولید شود.⁩

⁧صورت پروژه می‌خواهد حداقل ⁦5⁩ متریک کاربردی با کد جمع‌آوری شود. استفاده از ⁦Kibana⁩ یا ⁦OpenSearch Dashboards⁩ به‌تنهایی کافی نیست. ابزار کامل جمع‌آوری متریک‌ها، شامل ⁦latency⁩، ⁦throughput⁩، ⁦P95⁩، ⁦error rate⁩، اندازه ⁦index⁩، تعداد سند و مصرف ⁦CPU/memory⁩، باید قبل از اولین ⁦baseline⁩ در مرحله ⁦5⁩ آماده و استفاده شده باشد. این مرحله نقش آماده‌سازی و اعتبارسنجی قالب گزارش متریک‌ها را دارد: خروجی‌های اولیهٔ ⁦baseline⁩ را می‌خواند، ساختار ⁦JSON/CSV⁩ را کنترل می‌کند و اسکریپت تولید جدول و نمودار را آماده می‌کند. تولید نهایی جدول‌ها و نمودارهای کامل، چون به خروجی مرحله‌های ⁦8⁩ و ⁦9⁩ هم نیاز دارد، در بخش گزارش نهایی بعد از مرحله ⁦9⁩ انجام می‌شود.⁩

⁧متریک‌های پیشنهادی:⁩

- ⁧اندازه ⁦index⁩ روی دیسک.⁩
- ⁧تعداد ⁦document⁩های ذخیره‌شده.⁩
- ⁧زمان اجرای ⁦query⁩ها.⁩
- ⁧⁦throughput⁩ یا تعداد ⁦query⁩ در واحد زمان.⁩
- ⁧مصرف ⁦CPU⁩ و ⁦memory.⁩⁩
- P95 latency.
- error rate.

⁧خروجی مورد انتظار:⁩

- ⁧کد تولید جدول و نمودار، مثلاً ⁦`scripts/plot_metrics.py`⁩ یا ⁦`scripts/generate_report_tables.py`⁩، با استفاده از خروجی‌های ⁦`scripts/benchmark.py`⁩⁩
- ⁧فایل‌های خروجی در قالب ⁦JSON/CSV⁩⁩
- ⁧نمودارهای ⁦matplotlib⁩⁩
- ⁧جدول‌های قابل استفاده در گزارش نهایی⁩

⁧معیار اتمام:⁩

- ⁧فایل‌های متریک اولیهٔ تولیدشده توسط مرحله ⁦5⁩ خوانده و اعتبارسنجی شوند.⁩
- ⁧حداقل یک نمودار ⁦matplotlib⁩ نمونه برای ⁦baseline⁩ تولید شود تا مسیر گزارش‌سازی تست شود.⁩
- ⁧اسکریپت تولید جدول و نمودار طوری نوشته شود که بعد از مرحله‌های ⁦8⁩ و ⁦9⁩ بتواند خروجی کامل ⁦baseline/load-test/optimized⁩ را دوباره بخواند و گزارش نهایی بسازد.⁩
- ⁧تعریف محاسبه ⁦latency⁩، ⁦P95⁩، ⁦throughput⁩ و ⁦error rate⁩ در همه سناریوها یکسان باشد.⁩

⁧نتیجهٔ ثبت‌شدهٔ مرحله ⁦7:⁩⁩

- ⁧اسکریپت ⁦`scripts/plot_metrics.py`⁩ اضافه شده است.⁩
- ⁧گزارش ⁦`results/metrics_summary.json`⁩ با وضعیت ⁦`passed`⁩ موجود است و ⁦3⁩ ورودی را تجمیع کرده: ⁦`search_baseline.json`⁩، ⁦`hybrid_comparison.json`⁩ و ⁦`load_test_baseline.json`⁩.⁩
- ⁧خروجی‌های ⁦`results/metrics_summary.csv`⁩، ⁦`results/metrics_summary.md`⁩، ⁦`results/metrics_baseline_latency.png`⁩ و ⁦`results/metrics_dashboard.png`⁩ ساخته شده‌اند.⁩
- ⁧این مرحله برای داده‌های موجود کامل است، اما بعد از مرحله ⁦9⁩ باید روی خروجی‌های ⁦optimized⁩ هم دوباره اجرا شود تا جدول‌ها و نمودارهای نهایی تحویل ساخته شوند.⁩

## ⁧مرحله ⁦8:⁩ تست چندکلاینتی و فشار⁩

⁧وضعیت: انجام شده برای ⁦baseline⁩.⁩

⁧در این مرحله باید چند کاربر یا ⁦client⁩ همزمان شبیه‌سازی شوند. منظور از ⁦client⁩ می‌تواند ⁦thread⁩، ⁦process⁩، برنامه مستقل یا ابزار ⁦load testing⁩ باشد.⁩

⁧سناریوهای الزامی:⁩

- ⁧اجرای ⁦keyword search⁩ با ⁦1⁩، ⁦5⁩ و ⁦10⁩ کلاینت.⁩
- ⁧اجرای ⁦contain search⁩ با ⁦1⁩، ⁦5⁩ و ⁦10⁩ کلاینت.⁩
- ⁧اجرای ⁦fuzzy search⁩ با ⁦1⁩، ⁦5⁩ و ⁦10⁩ کلاینت.⁩
- ⁧اجرای معماری ترکیبی پیشنهادی با ⁦10⁩ کلاینت.⁩

⁧این‌ها جمعاً ⁦10⁩ سناریوی اجباری هستند.⁩

⁧مقادیر ⁦1⁩، ⁦5⁩ و ⁦10⁩ کلاینت اجباری‌اند و حذف نمی‌شوند. اما صورت پروژه می‌خواهد افت کارایی به‌صورت قابل توجه مشاهده شود. اگر در این ⁦10⁩ سناریوی اجباری افت محسوسی در ⁦latency⁩، ⁦P95⁩ یا ⁦throughput⁩ دیده نشد، باید سناریوهای افزایشی اختیاری هم اجرا شوند؛ مثلاً ⁦20⁩، ⁦50⁩ یا ⁦100⁩ کلاینت. این سناریوهای اضافی جایگزین سناریوهای اجباری نیستند و فقط برای پیدا کردن نقطه فشار سیستم استفاده می‌شوند.⁩

⁧قرارداد بار کاری هر سناریو:⁩

- ⁧برای هر نوع جست‌وجو یک ⁦query pool⁩ ثابت در فایل تعریف ⁦query⁩ ثبت می‌شود. ⁦keyword pool⁩ شامل ⁦`query optimization`⁩، ⁦`distributed database`⁩ و ⁦`learned indexes`⁩ با فیلتر ⁦`cs.DB`⁩ است؛ ⁦contain pool⁩ شامل ⁦`database systems`⁩، شرط ⁦`indexing`⁩ بدون ⁦`blockchain`⁩ و یکی از ⁦`query processing/storage engine/transaction processing`⁩ است؛ ⁦fuzzy pool⁩ شامل ⁦`databse optimiztion`⁩ و ⁦`transacton procesing`⁩ است؛ ⁦hybrid pool⁩ شامل سه ⁦query⁩ مقایسه‌ای مرحله ⁦6⁩ است.⁩
- ⁧در حالت پیش‌فرض وزن انتخاب ⁦query⁩ها داخل هر ⁦pool⁩ برابر است، مگر اینکه وزن‌ها صریحاً در فایل تعریف ⁦query⁩ ثبت شوند. همان وزن‌ها باید برای ⁦baseline⁩ و ⁦optimized⁩ ثابت بمانند.⁩
- ⁧انتخاب ⁦query⁩ برای هر درخواست با ⁦seed⁩ ثابت و مولد تصادفی قابل بازتولید انجام می‌شود؛ ⁦seed⁩، ترتیب تولید درخواست‌ها و نسخهٔ فایل ⁦query pool⁩ در خروجی هر سناریو ثبت می‌شود.⁩
- ⁧کلاینت‌ها بعد از پایان ⁦warm-up⁩ با یک مانع شروع هم‌زمان، مثل ⁦threading.Barrier⁩، وارد فاز اندازه‌گیری می‌شوند تا شروع تدریجی باعث تحریف ⁦throughput⁩ نشود.⁩
- ⁧مدل اجرا پیوسته است: هر کلاینت تا پایان مدت سناریو، بعد از دریافت پاسخ یا خطا، بلافاصله درخواست بعدی را از ⁦pool⁩ انتخاب و ارسال می‌کند. زمان فکر کاربر شبیه‌سازی نمی‌شود مگر اینکه در آزمایش جداگانه ثبت شود.⁩
- ⁧برای هر درخواست ⁦timeout⁩ ثابت، مثلاً ⁦10s⁩، تعریف می‌شود. ⁦timeout⁩ها به‌عنوان خطا ثبت می‌شوند و در ⁦error rate⁩ می‌آیند؛ ⁦latency⁩ درخواست‌های ناموفق جدا از درخواست‌های موفق گزارش می‌شود.⁩
- ⁧⁦connection pooling⁩ باید ثابت باشد: هر کلاینت یا ⁦thread⁩ از ⁦HTTP session/client⁩ پایدار خود استفاده می‌کند، یا یک ⁦pool⁩ مشترک با اندازه مشخص استفاده می‌شود. انتخاب نهایی باید در خروجی سناریو ثبت و بین ⁦baseline⁩ و ⁦optimized⁩ ثابت بماند.⁩
- ⁧هر سناریوی چندکلاینتی حداقل ⁦10s⁩ ⁦warm-up⁩ جدا از اندازه‌گیری دارد. فاز اندازه‌گیری طبق پروتکل مرحله ⁦5⁩ حداقل ⁦60s⁩ است و اگر تعداد نمونه ⁦latency⁩ کافی نباشد افزایش می‌یابد.⁩

⁧برای هر سناریو باید گزارش شود:⁩

- ⁧میانگین ⁦latency.⁩⁩
- P95 latency.
- throughput.
- error rate.
- ⁧مصرف منابع.⁩

⁧ابزارهای قابل استفاده:⁩

- ⁧کد چندنخی یا چندپردازه‌ای ⁦Python.⁩⁩
- Locust.
- k6.
- JMeter.

⁧برای این پروژه، کد ⁦Python⁩ چندنخی گزینه ساده‌تر و قابل کنترل‌تری است، مگر اینکه بعداً نیاز به گزارش ⁦load testing⁩ رسمی‌تر باشد.⁩

⁧خروجی مورد انتظار:⁩

- ⁧اسکریپت تست چندکلاینتی، مثلاً ⁦`scripts/run_load_tests.py`⁩⁩
- ⁧فایل نتایج هر سناریو⁩
- ⁧جدول مقایسه سناریوها⁩
- ⁧نمودار ⁦latency⁩ و ⁦throughput⁩⁩

⁧معیار اتمام:⁩

- ⁧هر ⁦10⁩ سناریو اجرا شود.⁩
- ⁧قرارداد بار کاری شامل ⁦query pool⁩، وزن‌ها، ⁦seed⁩، ⁦warm-up⁩، مدل اجرای پیوسته، ⁦timeout⁩، شروع هم‌زمان و ⁦connection pooling⁩ در خروجی هر سناریو ثبت شده باشد.⁩
- ⁧کاهش کارایی با افزایش ⁦client⁩ قابل مشاهده یا قابل تحلیل باشد.⁩
- ⁧اگر افت واضح مشاهده نشد، نتایج سناریوهای افزایشی ⁦20/50/100⁩ کلاینت یا دلیل اجرا نکردن آن‌ها ثبت شود.⁩
- ⁧خطاها و مصرف منابع ثبت شده باشند.⁩

⁧نتیجهٔ ثبت‌شدهٔ مرحله ⁦8:⁩⁩

- ⁧فایل قرارداد ⁦`queries/load_test_scenarios.json`⁩ موجود است.⁩
- ⁧اسکریپت ⁦`scripts/run_load_tests.py`⁩ اضافه شده است.⁩
- ⁧گزارش ⁦`results/load_test_baseline.json`⁩ با وضعیت ⁦`passed`⁩، ⁦`protocol_compliant: true`⁩ و ⁦`mandatory_scenarios_complete: true`⁩ موجود است.⁩
- ⁧خروجی‌های ⁦`results/load_test_baseline.csv`⁩، ⁦`results/load_test_measurements.jsonl.gz`⁩، ⁦`results/load_test_latency.png`⁩، ⁦`results/load_test_throughput.png`⁩ و ⁦`results/stage8_analysis.md`⁩ ساخته شده‌اند.⁩
- ⁧هر ⁦10⁩ سناریوی اجباری ⁦baseline⁩ اجرا شده‌اند و افت فشار قابل مشاهده ثبت شده است؛ بنابراین سناریوهای افزایشی ⁦20/50/100⁩ طبق گزارش لازم نشده‌اند.⁩

## ⁧مرحله ⁦9:⁩ بهینه‌سازی و مقایسه قبل/بعد⁩

⁧وضعیت: انجام شده و ⁦optimized index⁩ در محیط زندهٔ فعلی هم ساخته و بارگذاری شده است.⁩

⁧این مرحله باید بعد از اجرای ⁦baseline⁩ انجام شود. هدف این نیست که فقط تنظیمات را بهتر کنیم، بلکه باید نشان دهیم هر تغییر چه اثر مثبت و منفی داشته است. برای اینکه مقایسه آلوده نشود، ⁦baseline⁩ و ⁦optimized⁩ باید دو ⁦index⁩ جدا با ⁦mapping⁩ ثابت داشته باشند. ساخت و ورود داده به ⁦`arxiv_papers_optimized`⁩ در همین مرحله انجام می‌شود، نه در مرحله‌های ⁦3⁩ و ⁦4⁩.⁩

⁧تعریف مرز ⁦baseline⁩ و ⁦optimized:⁩⁩

- ⁧⁦baseline⁩ با ⁦index⁩ ثابت ⁦`arxiv_papers_baseline`⁩ اجرا می‌شود و ⁦mapping⁩ آن بعد از شروع ⁦benchmark⁩ تغییر نمی‌کند.⁩
- ⁧⁦optimized⁩ با ⁦index⁩ ثابت ⁦`arxiv_papers_optimized`⁩ اجرا می‌شود و تمام تغییرات بهینه‌سازی فقط در این ⁦index⁩ اعمال می‌شود.⁩
- ⁧⁦dataset⁩، نیت منطقی ⁦query set⁩، تعداد تکرارها، ⁦warm-up⁩، مدت آزمایش، ⁦seed⁩، تعداد ⁦client⁩ و ⁦resource limit⁩ها در هر دو حالت یکسان می‌مانند. ⁦DSL⁩، فیلد، ⁦analyzer⁩ و روش اجرایی هر ⁦query⁩ می‌تواند بین ⁦baseline⁩ و ⁦optimized⁩ متفاوت باشد، به شرط اینکه نیت جست‌وجو ثابت بماند و تفاوت اجرایی در فایل تعریف ⁦query⁩ همان نسخه ثبت شود.⁩
- ⁧اگر تغییری مثل ⁦multi-field⁩ در ⁦baseline⁩ آمده باشد، دیگر نباید دوباره به‌عنوان ⁦optimization⁩ شمرده شود. بنابراین ⁦multi-field⁩های مخصوص ⁦contain⁩ یا ⁦sort⁩ فقط در ⁦optimized⁩ تعریف می‌شوند، مگر اینکه از ابتدا صریحاً در ⁦baseline⁩ بوده باشند.⁩
- ⁧نتیجه هر ⁦benchmark⁩ باید نام ⁦index⁩، ⁦hash⁩ یا مسیر فایل ⁦mapping⁩، مسیر یا ⁦hash⁩ تعریف منطقی ⁦query⁩، و مسیر یا ⁦hash⁩ تعریف اجرایی ⁦query⁩ همان نسخه را ثبت کند.⁩

⁧بهینه‌سازی‌های قابل بررسی برای ⁦Elasticsearch:⁩⁩

- ⁧بهبود ⁦mapping⁩ و انتخاب دقیق نوع فیلدها.⁩
- ⁧اضافه‌کردن ⁦multi-field⁩ در ⁦optimized⁩ برای فیلدهایی که هم ⁦search⁩ و هم ⁦filter/sort⁩ لازم دارند، اگر در ⁦baseline⁩ وجود نداشته باشند.⁩
- ⁧بهبود ساختار ⁦query⁩ها بدون تغییر معنای منطقی جست‌وجو؛ توجه شود که ⁦filter context⁩ برای ⁦`year`⁩ و ⁦`categories`⁩ از ابتدا قرارداد ثابت است و در این پروژهٔ محلی به‌عنوان بهینه‌سازی قبل/بعد شمرده نمی‌شود.⁩
- ⁧طراحی ⁦analyzer⁩ مناسب برای ⁦contain search⁩ واژه/عبارت، بدون تغییر معنای منطقی ⁦query⁩.⁩
- ⁧مقایسهٔ جداگانهٔ ⁦wildcard-based substring search⁩ با ⁦ngram-based substring search⁩ با ورودی و معنای منطقی یکسان.⁩
- ⁧بررسی تعداد ⁦shard⁩ فقط در صورت نیاز و با آزمایش کنترل‌شده؛ ⁦replica⁩ در محیط محلی تک‌نودی ثابت ⁦`0`⁩ است و به‌عنوان بهینه‌سازی محلی آزمایش نمی‌شود.⁩
- ⁧تنظیم ⁦batch size⁩ در ⁦bulk ingestion.⁩⁩
- ⁧تنظیم ⁦refresh interval⁩ هنگام ⁦ingestion.⁩⁩

⁧بهینه‌سازی ورود داده و بهینه‌سازی جست‌وجو باید دو آزمایش جدا باشند:⁩

- ⁧⁦ingestion benchmark⁩ فقط اثر ⁦batch size⁩، ⁦refresh_interval⁩ و تنظیمات ⁦bulk⁩ را می‌سنجد. در محیط فعلی ⁦`number_of_replicas`⁩ همیشه ⁦`0`⁩ است. خروجی آن زمان ورود، خطاهای ⁦bulk⁩، اندازه ⁦index⁩ و تعداد سند است.⁩
- ⁧⁦search benchmark⁩ فقط اثر ⁦mapping⁩، ⁦analyzer⁩، ⁦ngram⁩، نوع ⁦query⁩ و در صورت تعریف آزمایش جدا اثر تعداد ⁦shard⁩ را می‌سنجد. ⁦filter context⁩ برای ⁦`year/categories`⁩ در همهٔ نسخه‌های قابل مقایسه ثابت است و اثر آن به‌عنوان بهینه‌سازی اصلی گزارش نمی‌شود. خروجی آن ⁦latency⁩، ⁦P95⁩، ⁦throughput⁩، تعداد نتایج، کیفیت و مصرف منابع است.⁩
- ⁧برای مقایسهٔ جست‌وجوی ⁦baseline/optimized⁩، هر دو ⁦index⁩ باید با پروتکل ورود دادهٔ یکسان ساخته شوند. آزمایش‌های ⁦batch size/refresh_interval⁩ روی ⁦index⁩های موقت جدا مثل ⁦`arxiv_papers_ingest_test_*`⁩ انجام می‌شوند و نتیجهٔ آن‌ها وارد مقایسهٔ جست‌وجوی ⁦baseline/optimized⁩ نمی‌شود.⁩
- ⁧اگر ⁦replica⁩ تحلیل شود، باید در محیط چندنودی جدا انجام شود و نتیجهٔ آن به‌عنوان آزمایش تکمیلی، نه بخشی از مقایسهٔ اصلی محیط محلی، گزارش شود.⁩

⁧روش درست اجرا:⁩

1. ⁧اجرای ⁦query⁩ها و تست‌ها با طراحی اولیه.⁩
2. ⁧ثبت ⁦baseline.⁩⁩
3. ⁧ساخت ⁦mapping⁩ بهینه‌شده و ⁦index⁩ جداگانه ⁦`arxiv_papers_optimized`⁩.⁩
4. ⁧ورود داده به ⁦index⁩ بهینه‌شده با پروتکل ثبت‌شده.⁩
5. ⁧اجرای آزمایش‌های تک‌متغیره یا ⁦ablation⁩ برای تغییرهای اصلی.⁩
6. ⁧اجرای دوبارهٔ همهٔ ⁦query⁩های مرحله ⁦5⁩، مقایسهٔ معماری ترکیبی مرحله ⁦6⁩، و ⁦10⁩ سناریوی چندکلاینتی اجباری مرحله ⁦8⁩ روی نسخهٔ بهینه‌شده، با همان پروتکل، همان ⁦seed⁩ و همان محدودیت منابع. اگر سناریوهای افزایشی ⁦20/50/100⁩ برای ⁦baseline⁩ اجرا شده باشند، همان سناریوها روی ⁦optimized⁩ هم اجرا شوند یا دلیل اجرا نکردنشان صریحاً ثبت شود.⁩
7. ⁧مقایسه ⁦before/after⁩ با جدول و نمودار.⁩
8. ⁧توضیح ⁦trade-off⁩ها، مثلاً افزایش حجم ⁦index⁩ در برابر کاهش ⁦latency.⁩⁩
9. ⁧اجرای دوبارهٔ اسکریپت تولید جدول و نمودار مرحله ⁦7⁩ روی همهٔ خروجی‌های مرحله‌های ⁦5⁩، ⁦8⁩ و ⁦9⁩ برای ساخت نمودارهای نهایی گزارش.⁩

⁧حداقل آزمایش‌های تک‌متغیره پیشنهادی:⁩

- ⁧فقط افزودن ⁦ngram⁩ برای آزمایش جداگانهٔ زیررشته، با ثابت‌بودن ورودی و معنای منطقی نسبت به نسخهٔ ⁦wildcard⁩.⁩
- ⁧فقط تغییر ⁦analyzer/mapping⁩ برای یک آزمایش کنترل‌شدهٔ جست‌وجو، بدون تغییر نیت منطقی ⁦query⁩ و با ثابت‌ماندن ⁦filter context⁩ برای ⁦year/categories⁩.⁩
- ⁧فقط تغییر تعداد ⁦shard⁩، در صورت نیاز و با ⁦index⁩ جدا و ثابت‌بودن بقیه تنظیمات؛ ⁦`number_of_replicas`⁩ در محیط محلی ثابت ⁦`0`⁩ می‌ماند.⁩
- ⁧فقط تنظیمات ورود داده مثل ⁦batch size⁩ و ⁦refresh_interval⁩ برای ⁦ingestion benchmark⁩.⁩
- ⁧نسخهٔ ترکیبی نهایی که چند بهینه‌سازی منتخب را با هم دارد.⁩

⁧خروجی مورد انتظار:⁩

- ⁧⁦mapping⁩ یا تنظیمات بهینه‌شده.⁩
- ⁧نتایج ⁦baseline⁩ و ⁦optimized.⁩⁩
- ⁧نتایج آزمایش‌های تک‌متغیره و نسخهٔ ترکیبی نهایی.⁩
- ⁧نتایج اجرای مجدد ⁦10⁩ سناریوی چندکلاینتی اجباری روی ⁦optimized⁩، و در صورت وجود، سناریوهای افزایشی مشابه ⁦baseline.⁩⁩
- ⁧نمودارهای مقایسه‌ای.⁩
- ⁧تحلیل نهایی اینکه کدام ⁦optimization⁩ ارزشمند بوده و کدام نه.⁩

⁧معیار اتمام:⁩

- ⁧مقایسه قبل/بعد با داده واقعی انجام شود.⁩
- ⁧تمام ⁦query⁩های اصلی دوباره اجرا شوند.⁩
- ⁧هر ⁦10⁩ سناریوی چندکلاینتی اجباری مرحله ⁦8⁩ روی ⁦optimized⁩ هم اجرا و با ⁦baseline⁩ مقایسه شود.⁩
- ⁧اگر سناریوهای افزایشی ⁦20/50/100⁩ در ⁦baseline⁩ اجرا شده‌اند، در ⁦optimized⁩ هم تکرار شوند یا دلیل عدم تکرار ثبت شود.⁩
- ⁧اثر هر تغییر اصلی با عدد و حداقل یک آزمایش تک‌متغیره پشتیبانی شود.⁩

⁧نتیجهٔ ثبت‌شدهٔ مرحله ⁦9:⁩⁩

- ⁧فایل ⁦`elasticsearch/mappings/arxiv_papers_optimized.json`⁩ و اسکریپت ⁦`scripts/index_optimized.py`⁩ موجود هستند.⁩
- ⁧گزارش ⁦`results/ingestion_optimized.json`⁩ با وضعیت ⁦`passed`⁩ موجود است و طبق آن ⁦50,000⁩ سند وارد ⁦`arxiv_papers_optimized`⁩ شده‌اند.⁩
- ⁧گزارش ⁦`results/search_optimized.json`⁩ با وضعیت ⁦`passed`⁩ و ⁦`protocol_compliant: true`⁩ موجود است؛ همهٔ ⁦9⁩ جست‌وجوی مرحله ⁦5⁩ با پروتکل ثابت دوباره اجرا شده‌اند.⁩
- ⁧گزارش ⁦`results/hybrid_comparison_optimized.json`⁩ و فایل‌های کیفیت ⁦`results/hybrid_quality_optimized.*`⁩ موجود هستند؛ طبق گزارش نهایی، ⁦Precision@10⁩ در همهٔ حالت‌ها نسبت به ⁦baseline⁩ ثابت مانده است.⁩
- ⁧گزارش ⁦`results/load_test_optimized.json`⁩ با وضعیت ⁦`passed`⁩، ⁦`protocol_compliant: true`⁩ و ⁦`mandatory_scenarios_complete: true`⁩ موجود است؛ هر ⁦10⁩ سناریوی چندکلاینتی اجباری روی ⁦optimized⁩ هم اجرا شده‌اند.⁩
- ⁧آزمایش‌های تک‌متغیره در ⁦`results/stage9_ablations.json`⁩ و مقایسهٔ قبل/بعد در ⁦`results/stage9_comparison.json`⁩ و ⁦`results/stage9_comparison.csv`⁩ ثبت شده‌اند.⁩
- ⁧نمودارهای ⁦matplotlib⁩ مقایسه‌ای ⁦`results/stage9_search_before_after.png`⁩ و ⁦`results/stage9_load_before_after.png`⁩ ساخته شده‌اند.⁩
- ⁧تحلیل مرحله در ⁦`results/stage9_analysis.md`⁩ ثبت شده است: ⁦ngram⁩ برای زیررشته سریع‌تر ولی پرهزینه‌تر از نظر حجم و زمان ⁦ingestion⁩ است؛ ⁦English analyzer⁩ به دلیل کندترشدن و تغییر ⁦recall⁩ رد شده؛ ⁦replica⁩ در محیط تک‌نودی آزمایش معتبر محسوب نشده و ⁦1 shard / 0 replica⁩ حفظ شده است.⁩
- ⁧نکتهٔ عملی: در بازبینی نهایی ⁦2026-08-08⁩، ⁦index⁩ زندهٔ ⁦`arxiv_papers_optimized`⁩ دوباره ساخته و با ⁦50,000⁩ سند بارگذاری شد. ⁦count⁩ مستقیم ⁦Elasticsearch⁩، جست‌وجوی نمونه و ⁦smoke test⁩ این وضعیت را تأیید کردند.⁩

## ⁧مرحله ⁦10:⁩ گزارش نهایی و تحویل⁩

⁧وضعیت: انجام شده؛ ⁦smoke test⁩ زنده بعد از بازسازی هر دو ⁦index⁩ پاس شده است.⁩

⁧این مرحله بعد از کامل‌شدن مرحله ⁦9⁩ انجام می‌شود و هدفش تبدیل همهٔ کدها، تنظیمات، خروجی آزمایش‌ها، نمودارها و تحلیل‌ها به یک تحویل قابل بازتولید و قابل دفاع است. بدون این مرحله، پروژه از نظر فنی اجرا شده ولی از نظر تحویل و گزارش کامل نیست.⁩

⁧ورودی‌های این مرحله:⁩

- ⁧دیتاست خام، دیتاست پاک‌شده و ⁦`dataset/DATASET.md`⁩.⁩
- ⁧مستند محیط ⁦Elasticsearch⁩، ⁦Docker Compose⁩ و تنظیمات منابع.⁩
- ⁧فایل‌های ⁦mapping⁩، تنظیمات ⁦index⁩، تعریف منطقی و اجرایی ⁦query⁩ها.⁩
- ⁧نتایج ⁦baseline⁩، معماری ترکیبی، قضاوت دستی کیفیت، تست‌های چندکلاینتی، آزمایش‌های تک‌متغیره و نسخهٔ ⁦optimized⁩.⁩
- ⁧جدول‌ها و نمودارهای تولیدشده با اسکریپت مرحله ⁦7⁩ بعد از کامل‌شدن مرحله ⁦9⁩.⁩

⁧خروجی مورد انتظار و فایل‌های تحویل:⁩

- ⁧کد ⁦Python⁩ مربوط به پیش‌پردازش داده.⁩
- ⁧کد ذخیره‌سازی دسته‌ای داده در پایگاه داده.⁩
- ⁧فایل تنظیمات، ⁦mapping⁩ یا توضیح ساختار ذخیره‌سازی.⁩
- ⁧کد اجرای سه نوع جست‌وجو.⁩
- ⁧پیاده‌سازی معماری ترکیبی جست‌وجو.⁩
- ⁧کد جمع‌آوری متریک‌ها و رسم نمودارها با ⁦matplotlib.⁩⁩
- ⁧کد اجرای آزمایش‌های چندکلاینتی.⁩
- ⁧فایل ⁦Docker Compose⁩ یا راهنمای کامل اجرای محیط پروژه.⁩
- ⁧فایل ⁦README⁩ برای اجرای مرحله‌به‌مرحله پروژه.⁩
- ⁧فایل وابستگی‌ها مثل ⁦`requirements.txt`⁩ یا ⁦`pyproject.toml`⁩.⁩
- ⁧ثبت تنظیمات محیط، نسخه‌ها، ⁦resource limit⁩ها و ⁦seed⁩ تصادفی ⁦benchmark⁩ها.⁩
- ⁧ثبت نشانی منبع اصلی دیتاست کنار هش فایل خام و پاک‌شده.⁩
- ⁧ثبت مسیر یا ⁦hash⁩ فایل‌های ⁦mapping⁩، تعریف منطقی ⁦query⁩ و تعریف اجرایی ⁦query⁩ برای هر نتیجهٔ ⁦benchmark.⁩⁩
- ⁧تست‌های جست‌وجو، ⁦aggregation⁩، حذف ⁦duplicate⁩، ⁦rank fusion⁩، آستانه اجرای ⁦fuzzy⁩ و محاسبه ⁦P95/throughput.⁩⁩
- ⁧گزارش نهایی شامل نتایج، متریک‌ها، مقایسه‌ها و تحلیل بهینه‌سازی‌ها.⁩

### ⁧بازتولیدپذیری و گزارش نهایی⁩

⁧این بخش در انتهای پروژه انجام می‌شود و تضمین می‌کند پروژه روی یک ماشین دیگر هم قابل اجرا و قابل بررسی باشد.⁩

⁧خروجی‌های لازم:⁩

- ⁧⁦`README.md`⁩ با دستورهای اجرای کامل از صفر: آماده‌سازی محیط، اجرای ⁦Docker Compose⁩، پاک‌سازی/اعتبارسنجی داده، ساخت ⁦index⁩، ورود داده، اجرای ⁦query⁩ها، اجرای ⁦benchmark⁩ها و تولید گزارش.⁩
- ⁧⁦`requirements.txt`⁩ یا ⁦`pyproject.toml`⁩ با نسخهٔ وابستگی‌های ⁦Python⁩.⁩
- ⁧ثبت نسخه‌های ⁦Docker⁩، ⁦Docker Compose⁩، ⁦Elasticsearch⁩، ⁦Python⁩ و کتابخانه‌های اصلی.⁩
- ⁧ثبت منبع اصلی دیتاست، مسیر فایل خام، مسیر فایل پاک‌شده، اندازه و ⁦SHA-256⁩ هر دو فایل.⁩
- ⁧ثبت مسیر یا ⁦hash⁩ فایل‌های ⁦mapping⁩، تعریف منطقی ⁦query⁩ و تعریف اجرایی ⁦query⁩ استفاده‌شده در همهٔ نتیجه‌ها.⁩
- ⁧تولید نهایی جدول‌ها و نمودارهای ⁦baseline⁩، ⁦load-test⁩ و ⁦optimized⁩ با اسکریپت مرحله ⁦7⁩، بعد از کامل‌شدن مرحله ⁦9⁩.⁩
- ⁧یک دستور یا اسکریپت smoke test که حداقل اتصال ⁦Elasticsearch⁩، وجود ⁦index⁩، تعداد سندها و اجرای یک ⁦query⁩ ساده را بررسی کند.⁩

⁧معیار اتمام:⁩

- ⁧یک نفر دیگر بتواند فقط با ⁦README.md⁩ و فایل‌های مخزن، محیط را بالا بیاورد و نتایج اصلی را بازتولید کند.⁩
- ⁧همهٔ فایل‌های نتیجه و گزارش مشخص کنند با کدام ⁦index⁩، ⁦mapping⁩، تعریف منطقی ⁦query⁩، تعریف اجرایی ⁦query⁩، ⁦seed⁩، نسخه و تنظیمات منابع تولید شده‌اند.⁩

⁧نتیجهٔ ثبت‌شدهٔ مرحله ⁦10:⁩⁩

- ⁧⁦`README.md`⁩ مسیر اجرای پروژه از نصب تا ⁦Elasticsearch⁩، ساخت ⁦index⁩ها، ورود داده، جست‌وجو، ⁦benchmark⁩، ⁦load test⁩، مقایسه و گزارش نهایی را پوشش می‌دهد.⁩
- ⁧⁦`requirements.txt`⁩ موجود و وابستگی‌ها ⁦pin⁩ شده‌اند.⁩
- ⁧گزارش نهایی در ⁦`reports/final-report.md`⁩ موجود است و خلاصهٔ داده، محیط، جست‌وجو، کیفیت، تست چندکاربره، بهینه‌سازی‌ها، محدودیت‌ها و دستورهای بازتولید را ثبت می‌کند.⁩
- ⁧اسکریپت‌های ⁦`scripts/generate_final_report.py`⁩، ⁦`scripts/build_reproducibility_manifest.py`⁩ و ⁦`scripts/smoke_test.py`⁩ موجود هستند.⁩
- ⁧فایل ⁦`results/reproducibility_manifest.json`⁩ با وضعیت ⁦`passed`⁩ موجود است و هش artifactهای ضروری، دیتاست‌ها، محیط، تنظیمات منابع و traceability شش ⁦benchmark⁩ اصلی را ثبت کرده است.⁩
- ⁧فایل ⁦`results/smoke_test.json`⁩ با اجرای زندهٔ جدید به‌روزرسانی شد و وضعیت آن ⁦`passed`⁩ است. این اجرا اتصال ⁦Elasticsearch 9.4.4⁩، سلامت ⁦cluster⁩، وجود هر دو ⁦index⁩، تعداد ⁦50,000⁩ سند در هر ⁦index⁩ و اجرای یک ⁦query⁩ واقعی را تأیید می‌کند.⁩

## ⁧ساختار پیشنهادی ادامه مخزن⁩

⁧ساختار فعلی ساده و مناسب شروع است. برای مراحل بعد بهتر است فایل‌ها به‌صورت زیر رشد کنند:⁩

```text
.
├── Q4.pdf
├── README.md
├── project.md
├── docker-compose.yml
├── requirements.txt
├── dataset/
│   ├── DATASET.md
│   ├── arxiv_project_sample_50k.jsonl
│   └── arxiv_project_sample_50k_cleaned.jsonl
├── elasticsearch/
│   ├── mappings/
│   │   ├── arxiv_papers_baseline.json
│   │   └── arxiv_papers_optimized.json
│   └── settings/
├── queries/
│   └── search_queries.json
├── scripts/
│   ├── clean_dataset.py
│   ├── validate_cleaned_dataset.py
│   ├── benchmark.py
│   ├── create_index.py
│   ├── index_dataset.py
│   ├── search_queries.py
│   ├── hybrid_search.py
│   ├── collect_metrics.py
│   ├── plot_metrics.py
│   └── run_load_tests.py
├── tests/
│   ├── test_clean_dataset.py
│   ├── test_search_queries.py
│   ├── test_hybrid_search.py
│   ├── test_indexing.py
│   └── test_benchmark_metrics.py
├── results/
│   ├── baseline/
│   ├── optimized/
│   ├── load-tests/
│   └── relevance_judgments.csv
└── reports/
    ├── figures/
    └── final-report.md
```

⁧این ساختار مرحله‌های پروژه را جدا نگه می‌دارد: داده در ⁦`dataset/`⁩، تنظیمات ⁦Elasticsearch⁩ در ⁦`elasticsearch/`⁩، کدهای اجرایی در ⁦`scripts/`⁩، خروجی آزمایش‌ها در ⁦`results/`⁩ و گزارش نهایی در ⁦`reports/`⁩.⁩

## ⁧نکات مهم اجرایی⁩

- ⁧دیتاست خام نباید در مراحل بعد تغییر کند.⁩
- ⁧منبع ورودی همه مراحل بعدی باید ⁦`dataset/arxiv_project_sample_50k_cleaned.jsonl`⁩ باشد.⁩
- ⁧⁦`title_abstract`⁩ برای جست‌وجوی عمومی بهترین فیلد شروع است، اما برای ⁦query⁩های دقیق‌تر باید ⁦`title`⁩ و ⁦`abstract`⁩ هم جداگانه استفاده شوند.⁩
- ⁧⁦`categories`⁩ و ⁦`year`⁩ باید از همان ⁦baseline⁩ در ⁦filter context⁩ استفاده شوند، نه ⁦query scoring⁩. این تصمیم قرارداد ثابت مقایسه است و نباید بعداً به‌عنوان ⁦optimization⁩ شمرده شود.⁩
- ⁧⁦fuzzy search⁩ باید کنترل‌شده اجرا شود، چون می‌تواند ⁦latency⁩ را بالا ببرد.⁩
- ⁧هر ⁦benchmark⁩ بدون ثبت نسخه ⁦Elasticsearch⁩، ⁦mapping⁩، تعریف منطقی و اجرایی ⁦query⁩، ⁦analyzer⁩، تعداد ⁦shard/replica⁩ و ⁦resource limit⁩ قابل دفاع نیست.⁩
- ⁧در محیط ⁦single-node⁩ فعلی، ⁦`number_of_replicas`⁩ باید ⁦`0`⁩ بماند؛ آزمایش ⁦replica⁩ فقط در کلاستر چندنودی جدا معنی‌دار است.⁩
- ⁧⁦benchmark⁩ قبل/بعد فقط وقتی معتبر است که پروتکل اندازه‌گیری، ⁦seed⁩، ⁦query set⁩، تعداد تکرارها و محیط اجرا ثابت باشد.⁩
- ⁧⁦score⁩ خام روش‌های مختلف جست‌وجو نباید مستقیم جمع شود؛ برای ترکیب روش‌ها باید از نرمال‌سازی رتبه یا ⁦rank fusion⁩ استفاده شود.⁩
- ⁧برای گزارش نهایی، فقط نمودار کافی نیست؛ باید تحلیل شود که چرا یک روش سریع‌تر، کندتر، دقیق‌تر یا پرهزینه‌تر شده است.⁩

## ⁧تعریف ⁦done⁩ برای کل پروژه⁩

⁧پروژه زمانی کامل است که:⁩

- ⁧دیتاست پاک‌شده معتبر باشد.⁩
- ⁧⁦Elasticsearch⁩ یا تکنولوژی انتخابی با ⁦Docker Compose⁩ قابل اجرا باشد.⁩
- ⁧⁦mapping⁩ و تنظیمات ذخیره‌سازی مستند باشند.⁩
- ⁧⁦50⁩,⁦000⁩ سند با ⁦bulk insert⁩ وارد شده باشند.⁩
- ⁧⁦query⁩های اجباری و ⁦query⁩های مقایسه‌ای اجرا شده باشند.⁩
- ⁧معماری ترکیبی پیاده‌سازی و با روش‌های پایه مقایسه شده باشد.⁩
- ⁧برای هر ⁦query/method⁩ دقیقاً ⁦10⁩ نتیجهٔ اول در صورت وجود حداقل ⁦10⁩ نتیجه، و در غیر این صورت تمام نتایج موجود قضاوت شده باشد؛ تعداد قضاوت‌ها در حالت کامل ⁦120⁩ است و ⁦Precision@10⁩ با مدیریت رتبه‌های خالی گزارش شده باشد.⁩
- ⁧حداقل ⁦5⁩ متریک با کد جمع‌آوری شده باشد.⁩
- ⁧حداقل یک نمودار ⁦matplotlib⁩ در گزارش آمده باشد.⁩
- ⁧⁦10⁩ سناریوی چندکلاینتی اجباری هم روی ⁦baseline⁩ و هم روی ⁦optimized⁩ اجرا و مقایسه شده باشد.⁩
- ⁧اگر ⁦10⁩ سناریوی اجباری افت محسوسی نشان ندادند، سناریوهای بار بیشتر اجرا یا دلیل عدم اجرا ثبت شده باشد؛ اگر این سناریوهای افزایشی برای ⁦baseline⁩ اجرا شدند، برای ⁦optimized⁩ هم تکرار یا دلیل عدم تکرار ثبت شود.⁩
- ⁧بهینه‌سازی‌ها با مقایسه عددی قبل/بعد و آزمایش تک‌متغیره تحلیل شده باشند.⁩
- ⁧⁦README.md⁩، فایل وابستگی‌ها و ثبت منبع/هش دیتاست کامل شده باشند.⁩
- ⁧گزارش نهایی، دستور اجرای پروژه و همه کدهای لازم داخل مخزن باشند.⁩
