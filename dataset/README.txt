Each line in this file is one JSON object.
The file format is JSON Lines (.jsonl).
Students must read and preprocess this file using Python before inserting the data into their selected database/search engine.

Note: arxiv_project_sample_50k.jsonl is larger than GitHub's 100 MB file limit
and is tracked with Git LFS.
